---
import MainLayout from "../layouts/MainLayout.astro"

type Props = MarkdownLayoutProps<{
  /* posted?: Date; // todo get this from git! */
  /* updated?: Date; // todo get this from git! */
  title: string;
  description: string;
  author?: string;
  imageURL?: string;
  tags?: string[];// todo: can split on ','
}>;

const cannonicalURL = Astro.canonicalURL;
const { frontmatter: { title, description, imageURL, author, tags } } = Astro.props as Props;
---

<MainLayout title={title} tags={tags} imageURL={imageURL} description={description} type="article" author={author}>
  <article class="prose container mx-auto px-4 py-6">
    <slot/>
  </article>
</MainLayout>
